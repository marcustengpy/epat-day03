git init

git add *

git commit -m "initial commit"

git remote add origin <git repo url>

git branch -m main

git push -u origin

git checkout -b develop main

git add *

git status

git commit -m "added readme.txtasda
--> create new file mytestfile

